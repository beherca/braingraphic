
Peer = Utils.cls.extend(Observable, {
   
});
/**
 * Monitor peers' communication on frame level,
 * this is a interface to communicate with NDCP (Node Distributed compute platform) to 
 */
PeersManager = Utils.cls.extend(Observable, {
   
});